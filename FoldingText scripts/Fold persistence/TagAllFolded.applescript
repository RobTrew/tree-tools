-- Tag Collapsed nodes with @foldon run	tell application "FoldingText"		set lstDocs to documents		if length of lstDocs < 1 then return				tell item 1 of lstDocs			set {dlm, my text item delimiters} to {my text item delimiters, ","}			set strFoldNodes to (read JSON (body of (read JSON (HTTP request URI "/view/collapsed.json")))) as string			set my text item delimiters to dlm						HTTP request method "PATCH" URI "/nodes/" & strFoldNodes & ".json" body (make JSON {addTags:{fold:""}})		end tell	end tellend run